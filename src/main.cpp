// #include"linkedList.h"
#include"Polynomial.h"
#include"Parser.h"
#include "Menu.h"
using std::cout; using std::endl;
int main(){
    if(true){
        /* debug part*/
       /* 
        Polynomial link;
        link.insert(1, 2);
        link.insert(3, 1.4);
        // Node* n = link.head;
        // Polynomial link2;
        // link2.insert(1, 1);
        // link2.insert(2, 3);
        // size_t sz;
        // Node* l = merge(link.head, link2.head, [](int v1, int v2){return v1+v2;}, sz);
        // while(n != nullptr){
        //     cout << n << ','<< n->key << ',' << n->value<< endl;
        //     cout << n2 << ','<< n2->key << ',' << n2->value << endl;
        //     n = n->next;
        //     n2 = n2->next;
        // }
        link.printPoly(std::cout);
        // link = link*link;
        // auto n = link.index(3);
        
        // n->value = 4;
        // n->key = 2;
        // if(n == nullptr)
        //     link.printPoly();
        // link2.printPoly();
        // Polynomial lk = link*link;

        // lk.insert(0, 3);
        // lk.printPoly();
        // lk.derive();
        // lk.printPoly();
        // cout << lk.compute(1) << endl;
        // for(int i = 0; i < 10; i++){
        //     lk.derive();
        // }
        // lk.printPoly();
        // link *= link2;
        // link.printPoly();
        // link *= lk;
        // link.printPoly();
        // link.removeVal(0);
        // link.remove(3);
        // cout <<  link.length() << endl;
        */
        // auto poly = PolyParser::generatePoly("6x^(-3) + (-x^2) + 4.4x^2 + - 1.2x^9");
        // poly ->printPoly(std::cout);
        // link = link - *poly;
        // link.printPoly(std::cout);
        // poly->derive();
        // poly->printPoly(std::cout);
        // // auto temp = poly;
        // *poly = *poly * *poly; 
        // poly->printPoly(std::cout);

        // delete temp;
        // delete poly;
        /*
        */
       Menu menu;
       menu.mainMenu();
    }else{
        /* implementation part*/
    }
}